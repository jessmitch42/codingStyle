var age = 26;
var name = "Jess";

// white space!

var num = 10;

if (num > 0) { // always have curly brackets for if/else/for/while
// don't techinically need curly's for one command but it's proper syntax
    console.log("The number is greater than zero.");
}
console.log("good job!");

// functions
function doSomething(arg1, arg2) {
    // code here;
}

var doSomething = function(arg1, arg2) {
    // code here;
}

var doSomething = function doSomething(arg1, arg2) {
    // code here;
}

/* multiple line comments
functions above are not all equal.
Use first version
*/



    